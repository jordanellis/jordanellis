{
  "name": "react-resume",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "author": "Jordan Ellis",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
